import { Tabs } from "@mantine/core"

const BoardPermissionSettings = () => {
  return (
    <Tabs.Panel value="permissions" p="sm">
      Permissions settings
    </Tabs.Panel>
  )
}
export default BoardPermissionSettings
